//Fails to compile because a Java Source code file with a public interface or class must be named the same.

public interface Printable {
  void doSomething();
}

interface Movable {
  void doSomethingElse();
}

